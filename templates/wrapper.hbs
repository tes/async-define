define({{#if bundleName}}"{{bundleName}}", {{/if}}{{#if depsStr}}[{{{depsStr}}}], {{/if}}function ({{{depsArgs}}}) {
  {{#each importVar}}
    {{#each this}}
      var {{this}} = ___{{@../key}}.{{this}};
    {{/each}}
  {{/each}}

  __placeholder__

  return {
    {{#each exportVar}}
      {{this}}: {{this}},
    {{/each}}
  };
});
